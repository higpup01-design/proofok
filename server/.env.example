# Base URL your customers will visit (no trailing slash)
BASE_URL=http://127.0.0.1:5000

# Email settings
SMTP_HOST=smtp.colormagic.biz
SMTP_PORT=587
SMTP_USER=YOUR_SMTP_USERNAME
SMTP_PASS=YOUR_SMTP_PASSWORD
FROM_EMAIL=PROOFS@colormagic.biz
TO_EMAIL=orders@colormagic.biz

# Set true if your SMTP uses implicit SSL (port 465)
SMTP_SSL=false
